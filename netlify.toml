[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback — every URL serves index.html so TanStack Router handles routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
